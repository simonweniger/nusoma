import * as React from 'react';

import { LeadGenerationSkeletonCard } from '~/components/organizations/slug/home/lead-generation-skeleton-card';

export default function LeadGenerationLoading(): React.JSX.Element {
  return <LeadGenerationSkeletonCard />;
}
