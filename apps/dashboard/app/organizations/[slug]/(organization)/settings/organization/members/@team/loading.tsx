import * as React from 'react';

import { MembersSkeletonCard } from '~/components/organizations/slug/settings/organization/members/members-skeleton-card';

export default function TeamLoading(): React.JSX.Element {
  return <MembersSkeletonCard />;
}
