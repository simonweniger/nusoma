import * as React from 'react';

import { MultiFactorAuthenticationSkeletonCard } from '~/components/organizations/slug/settings/account/security/multi-factor-authentication-skeleton-card';

export default function MultiFactorAuthenticationLoading(): React.JSX.Element {
  return <MultiFactorAuthenticationSkeletonCard />;
}
