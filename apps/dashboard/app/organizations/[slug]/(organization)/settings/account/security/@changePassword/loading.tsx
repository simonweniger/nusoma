import * as React from 'react';

import { ChangePasswordSkeletonCard } from '~/components/organizations/slug/settings/account/security/change-password-skeleton-card';

export default function ChangePasswordLoading(): React.JSX.Element {
  return <ChangePasswordSkeletonCard />;
}
