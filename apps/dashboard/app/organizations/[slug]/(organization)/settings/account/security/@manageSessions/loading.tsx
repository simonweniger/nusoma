import * as React from 'react';

import { SessionsSkeletonCard } from '~/components/organizations/slug/settings/account/security/sessions-skeleton-card';

export default function ManageSessionsLoading(): React.JSX.Element {
  return <SessionsSkeletonCard />;
}
