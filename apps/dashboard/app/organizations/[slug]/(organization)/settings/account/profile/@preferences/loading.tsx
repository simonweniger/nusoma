import * as React from 'react';

import { PreferencesSkeletonCard } from '~/components/organizations/slug/settings/account/profile/preferences-skeleton-card';

export default function PreferencesLoading(): React.JSX.Element {
  return <PreferencesSkeletonCard />;
}
