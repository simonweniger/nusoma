import * as React from 'react';

import { TransactionalEmailsSkeletonCard } from '~/components/organizations/slug/settings/account/notifications/transactional-emails-skeleton-card';

export default function TransactionalEmailsLoading(): React.JSX.Element {
  return <TransactionalEmailsSkeletonCard />;
}
