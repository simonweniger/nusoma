import { AnimatedEdge } from './animated';
import { MorphingEdge } from './morphing';
import { TemporaryEdge } from './temporary';

export const edgeTypes = {
  animated: AnimatedEdge,
  morphing: MorphingEdge,
  temporary: TemporaryEdge,
};
